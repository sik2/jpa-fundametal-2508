spring:
  application:
    name: jpaFundamental2508
  profiles:
    active: dev
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # SQL ??? ??
        highlight_sql: true # SQL ?? ??
        use_sql_comments: true # SQL? ?? ??
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
